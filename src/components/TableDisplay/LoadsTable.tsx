import { BeamState, Load } from '../../types/staticAnalysis'

type LoadsTableProps = {
  loads: Load[]
}

function LoadsTable({ loads }: LoadsTableProps) {
  return <div>LoadsTable</div>
}

export default LoadsTable
